using UnityEngine;

public static class InventoryTypes
{
    public enum InventoryItemSize { x64x64, x128x64, x128x128, x256x128 }
    public enum InventoryItemType { Passive, ActiveToClick, ActiveOne, ActiveTwo, ActiveThree, ActiveFour }
}